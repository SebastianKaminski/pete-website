<div>
	<label for="">
		Number of sections
		<input type="number" step="1" min="3" max="30">
	</label>
</div>

<div>
	<?php
		$finishes = Mage::getResourceModel('catalog/product_collection');
		// var_dump($finishes);
		foreach ($finishes as $item) {
			echo $finish = Mage::getModel('catalog/product')->loadByAttribute('item_type','Finish');
			// echo "<option value=".$finish->getPrice().">".$finish->getName()."</option>";
		}
	?>
	<label for="">
		Finish
		<select name="" id="">
			<option value="false" selected="selcted" disabled="disabled">Choose finish</option>
		</select>
	</label>
</div>